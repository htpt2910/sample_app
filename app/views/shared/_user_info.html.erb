<%= link_to gravatar_for(current_user, size: Settings.post.avatar_size), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t("post.view_profile"), current_user %></span>
<span><%= pluralize(current_user.microposts.count, t("post.microposts")) %></span>
