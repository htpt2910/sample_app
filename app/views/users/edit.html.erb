<% provide(:title, t("edit.edit_user")) %>
<% provide(:button_text, t("edit.save")) %>
<h1><%= t("edit.cheering") %></h1>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render "form" %>
    <div class="gravatar_edit">
      <%= gravatar_for @user %>
      <a><%= link_to t("edit.change"), Settings.gravatar.email %></a>
    </div>
  </div>
</div>
